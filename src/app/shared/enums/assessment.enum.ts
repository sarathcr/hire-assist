export enum AssessmentRoundsEnum {
  Completed = 'completed',
  Pending = 'pending',
  Skip = 'skip',
}

export enum AssessmentStatusEnum {
  Active = 'active',
  Pending = 'pending',
  OnReview = 'onReview',
  NotAttended = 'notAttended',
  Skip = 'skip',
  Saved = 'saved',
  Completed = 'completed',
  Selected = 'selected',
  Rejected = 'rejected',
  Scheduled = 'scheduled',
}
